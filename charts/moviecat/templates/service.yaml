apiVersion: v1
kind: Service
metadata:
  name: {{ include "moviecat.fullname" . }}
  labels:
    {{- include "moviecat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "moviecat.selectorLabels" . | nindent 4 }}

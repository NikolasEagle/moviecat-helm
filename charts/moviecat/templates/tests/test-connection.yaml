apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moviecat.fullname" . }}-test-connection"
  labels:
    {{- include "moviecat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "moviecat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
